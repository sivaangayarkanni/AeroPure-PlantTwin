[project]
name = "aeropure-plant-twin"
version = "1.0.0"
description = "Industry digital twin for AeroPure modular exhaust resource recovery"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.110", "uvicorn>=0.27", "pydantic>=2.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
